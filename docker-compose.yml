version: '3.8'

services:
  postgres:
    image: postgres:15-alpine
    container_name: influencerium-postgres
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: influencerium
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - influencerium-network

  redis:
    image: redis:7-alpine
    container_name: influencerium-redis
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - influencerium-network

  pgadmin:
    image: dpage/pgadmin4:latest
    container_name: influencerium-pgadmin
    environment:
      PGADMIN_DEFAULT_EMAIL: admin@influencerium.com
      PGADMIN_DEFAULT_PASSWORD: admin
    ports:
      - "5050:80"
    depends_on:
      - postgres
    networks:
      - influencerium-network

  redis-commander:
    image: rediscommander/redis-commander:latest
    container_name: influencerium-redis-commander
    environment:
      - REDIS_HOSTS=local:redis:6379
    ports:
      - "8081:8081"
    depends_on:
      - redis
    networks:
      - influencerium-network

volumes:
  postgres_data:
  redis_data:

networks:
  influencerium-network:
    driver: bridge
